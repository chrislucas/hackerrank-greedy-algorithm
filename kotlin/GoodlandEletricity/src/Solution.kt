/**
 * https://www.hackerrank.com/challenges/pylons/problem?utm_campaign=challenge-recommendation&utm_medium=email&utm_source=24-hour-campaign
 * */


// Complete the pylons function below.
fun pylons(k: Int, arr: Array<Int>): Int {
    var count_0 = 0
    var acc = 0
    var i = 0

    return acc
}

fun main(args: Array<String>) {
    val k = readLine()!!.split(" ").map { i -> i.toInt() }.toTypedArray()
    val arr = readLine()!!.split(" ").map { i -> i.toInt()}.toTypedArray()
    println(pylons(k[1], arr))
}